/**
 * The connector/pglite module.
 * @packageDocumentation
 * @module connector/pglite
 * @since v0.0.0
 */
/// connector/pglite

/**
 * The DatabaseDataOptions type configures the table and column names used for
 * data storage in PGlite.
 * @category PGlite Connector
 * @since v0.0.0
 */
/// DatabaseDataOptions

/**
 * The PgliteDataConnector interface describes a DataConnector that wraps a
 * PGlite instance.
 * @category PGlite Connector
 * @since v0.0.0
 */
/// PgliteDataConnector

/**
 * The createPgliteDataConnector function creates a PgliteDataConnector with
 * optional table settings.
 * @category PGlite Connector
 * @since v0.0.0
 */
/// createPgliteDataConnector

/**
 * The DatabaseMetaOptions type configures the table and column names used for
 * metadata storage in PGlite.
 * @category PGlite Connector
 * @since v0.0.0
 */
/// DatabaseMetaOptions

/**
 * The PgliteMetaConnector interface describes a MetaConnector that wraps a
 * PGlite instance.
 * @category PGlite Connector
 * @since v0.0.0
 */
/// PgliteMetaConnector

/**
 * The createPgliteMetaConnector function creates a PgliteMetaConnector with
 * optional table settings.
 * @category PGlite Connector
 * @since v0.0.0
 */
/// createPgliteMetaConnector
